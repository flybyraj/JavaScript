let heros=["superman","ironman","thor","hulk","shaktiman"];

//for Loop
for(let idx=0;idx<heros.length;idx++) {
    console.log(heros[idx]);
}

//for of
for (let el of heros) {
    console.log(el);
}

//for of
let cities=["delhi","pune","mumbai","gurgaon"];

for (let city of cities) {
    console.log(city.toUpperCase()); //toUpperCase
}